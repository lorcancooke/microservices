spring:
  application:
    name: config-server
  profiles:
    active:
      - git
  cloud:
    config:
      server:
        #native:
        #search-locations: classpath:/config
        git:
          uri: https://github.com/lorcancooke/config.git
          default-label: main
          timeout: 5
          clone-on-start: true
          force-pull: true
server:
  port: 8888

encrypt:
  key: fje83Ki8403Iod87dne7Yjsl3THueh48jfuO9j4U2hf64Lo


#Enable all Spring Boot Actuator endpoints.
management:
  endpoints:
    web:
      exposure:
        include: "*"